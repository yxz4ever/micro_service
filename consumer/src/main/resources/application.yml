logging:
    pattern:
        dateformat: MM-dd HH:mm:ss:SSS
spring:
    rabbitmq:
        host: 127.0.0.1 # rabbitMQ的ip地址
        port: 5672 # 端口
        username: luo
        password: 123123
        virtual-host: /
        listener:
            simple:
                prefetch: 1 # 预读上限